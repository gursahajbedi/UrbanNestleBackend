{
    "builds":[{
        "src":"realest_estate/wsgi.py",
        "use":"@vercel/python",
        "config":{"maxLamdaSize": "15mb", "runtime":"python3.9" }
    }],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"realest_estate/wsgi.py"
        }
    ]
}
